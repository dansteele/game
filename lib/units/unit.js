// import "zustand";

// const Unit = (Kind, health, baseDamage, enemy) => {
//   const health = useStore((state) => state.setHealth(health));
//   const baseDamage = useStore((state) => state.setBaseDamage(baseDamage));
//   const enemy = useStore((state) => state.setEnemy(enemy));

//   return <Kind store={useStore} />;
// };
